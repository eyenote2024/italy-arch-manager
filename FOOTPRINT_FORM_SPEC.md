# FootprintForm 元件設計規格文件

**文件版本**: 1.0  
**最後更新**: 2026-01-01  
**負責人**: EYENOTE STUDIO  
**元件路徑**: `/src/App.jsx` (Lines 10-151)

---

## 目錄
1. [元件概述](#元件概述)
2. [功能說明](#功能說明)
3. [UI 設計規範](#ui-設計規範)
4. [資料結構](#資料結構)
5. [互動邏輯](#互動邏輯)
6. [技術實作](#技術實作)

---

## 元件概述

### 用途
FootprintForm 是一個彈窗表單元件,用於讓使用者記錄參觀建築時的感受和情緒標籤。

### 使用場景
- 使用者點擊建築卡片上的「留下足跡」按鈕
- 編輯已存在的足跡記錄

### 核心功能
- 記錄文字感受 (最多 500 字)
- 選擇情緒標籤
- 自動填入建築金句作為初始內容
- 儲存/刪除/取消操作

---

## 功能說明

### 新增足跡
1. 使用者點擊「留下足跡」按鈕
2. 彈窗顯示,留言框自動填入該建築的第一條金句
3. 使用者可編輯留言內容
4. 選擇情緒標籤 (選填)
5. 點擊「儲存足跡」完成記錄

### 編輯足跡
1. 使用者點擊已有足跡的建築
2. 彈窗顯示,留言框顯示原有內容
3. 使用者可修改留言和情緒標籤
4. 可選擇「刪除」或「儲存足跡」

---

## UI 設計規範

### 整體佈局

```
┌─────────────────────────────────────┐
│ Header                              │
│ - 建築名稱 - 留下足跡/我的足跡      │
│ - 時間戳記                          │
├─────────────────────────────────────┤
│ 留言框區域                          │
│ - Label: 寫下你的感受...            │
│ - Textarea (160px 高)               │
│ - 字數計數器 (右下)                 │
├─────────────────────────────────────┤
│ 情緒標籤區域 (間距 0.5rem)          │
│ - Label: 😊 情緒標籤                │
│ - 按鈕群組 (可換行)                 │
├─────────────────────────────────────┤
│ 操作按鈕                            │
│ [刪除] [取消] [儲存足跡]            │
└─────────────────────────────────────┘
```

### 色彩規範

| 元素 | 顏色代碼 | 說明 |
|------|----------|------|
| 主要強調色 | `var(--accent-gold)` | 標題、選中狀態 |
| 背景色 | `#1a1a1a` | Textarea 背景 |
| 邊框色 | `#333` | 一般邊框 |
| 次要邊框 | `#444` | 按鈕邊框 |
| 主要文字 | `#fff` | 輸入文字 |
| 次要文字 | `#ccc` | Label 文字 |
| 提示文字 | `#888` | 時間戳記 |
| 淡化文字 | `#666` | 字數計數 |
| 刪除按鈕 | `#d32f2f` | 警告色 |

### 字體規範

| 元素 | 字體大小 | 字重 |
|------|----------|------|
| 標題 | 1.3rem | normal |
| 時間戳記 | 0.85rem | normal |
| Label | 0.9rem | normal |
| Textarea | 0.9rem | normal |
| 情緒按鈕 | 0.85rem | normal |
| 操作按鈕 | 0.9rem | 600 (儲存) |
| 字數計數 | 0.75rem | normal |

### 間距規範

| 區域 | 屬性 | 數值 |
|------|------|------|
| 整體容器 | color | #fff |
| Header | marginBottom | 1.5rem |
| Header | paddingBottom | 1rem |
| Header | borderBottom | 1px solid #333 |
| 標題 h2 | marginBottom | 0.5rem |
| 留言框區域 | marginBottom | 0.5rem |
| Label | marginBottom | 0.5rem |
| Textarea | padding | 0.8rem |
| 字數計數 | marginTop | 0.3rem |
| 情緒標籤區域 | marginBottom | 2rem |
| 情緒 Label | marginBottom | 0.8rem |
| 情緒按鈕 | gap | 0.5rem |
| 情緒按鈕 | padding | 0.5rem 1rem |
| 操作按鈕 | gap | 0.5rem |
| 操作按鈕 | padding | 0.8rem |

### 互動樣式

| 元素 | 屬性 | 說明 |
|------|------|------|
| 所有按鈕 | cursor | pointer |
| 情緒按鈕 | transition | all 0.3s ease |
| Textarea | resize | vertical (可垂直調整) |

### 時間格式

```javascript
{
  year: 'numeric',      // 2026
  month: '2-digit',     // 01
  day: '2-digit',       // 01
  hour: '2-digit',      // 22
  minute: '2-digit'     // 35
  // 注意: 未設定 hour12,預設依系統設定
}
```
顯示範例: `2026/01/01 下午10:35` 或 `2026/01/01 22:35`

---

## 資料結構

### Props

```typescript
interface FootprintFormProps {
  arch: Architecture;           // 建築物件
  existingFootprint?: Footprint; // 已存在的足跡 (編輯模式)
  onSave: (note: string, mood: string) => void;
  onDelete: () => void;
  onCancel: () => void;
}
```

### Architecture 物件

```typescript
interface Architecture {
  id: string;
  name: string;
  quotes: Array<{
    text: string;
    source: string;
  }>;
  // ... 其他屬性
}
```

### Footprint 物件

```typescript
interface Footprint {
  archId: string;
  archName: string;
  cityName: string;
  timestamp: number;
  note: string;
  mood: string;
}
```

---

## 互動邏輯

### 初始化邏輯

```javascript
// 留言內容初始化
const initialNote = existingFootprint?.note || arch?.quotes?.[0]?.text || '';

// 情緒標籤初始化
const initialMood = existingFootprint?.mood || '';
```

### 情緒選項

```javascript
const moods = ['震撼', '平靜', '浪漫', '孤獨', '感動', '其餘'];
```

### 驗證規則

- **必填欄位**: 留言內容 (note)
- **選填欄位**: 情緒標籤 (mood)
- **字數限制**: 500 字

### 儲存流程

1. 檢查留言內容是否為空
2. 如果為空,顯示提示: `請寫下您的感受`
3. 如果有內容,呼叫 `onSave(note, mood)`
4. 關閉彈窗

### 刪除流程

1. 僅在編輯模式 (`existingFootprint` 存在) 顯示刪除按鈕
2. 點擊刪除按鈕
3. 呼叫 `onDelete()`
4. 關閉彈窗

---

## 技術實作

### 狀態管理

```javascript
const [note, setNote] = useState(initialNote);
const [mood, setMood] = useState(initialMood);
```

### Debug 日誌

```javascript
useEffect(() => {
  console.log("🛠️ FootprintForm Mounted for:", arch?.name);
}, [arch]);
```
此 log 用於開發時追蹤元件掛載狀態,可在生產環境移除。

### 關鍵樣式

#### Textarea
```javascript
{
  width: '100%',
  minHeight: '160px',
  padding: '0.8rem',
  backgroundColor: '#1a1a1a',
  border: '1px solid #333',
  borderRadius: '6px',
  color: '#fff',
  fontSize: '0.9rem',
  lineHeight: '1.6',
  resize: 'vertical',
  fontFamily: 'inherit'
}
```

#### 情緒按鈕 (選中狀態)
```javascript
{
  padding: '0.5rem 1rem',
  borderRadius: '20px',
  border: '2px solid var(--accent-gold)',
  background: 'rgba(212, 175, 55, 0.2)',
  color: 'var(--accent-gold)',
  fontSize: '0.85rem',
  cursor: 'pointer',
  transition: 'all 0.3s ease'
}
```

#### 儲存按鈕
```javascript
{
  flex: 1,
  padding: '0.8rem',
  borderRadius: '6px',
  border: 'none',
  background: 'var(--accent-gold)',
  color: '#000',
  fontSize: '0.9rem',
  fontWeight: '600',
  cursor: 'pointer'
}
```

### Placeholder 文字

```
下午三點的陽光從玫瑰窗灑進來,那一刻我想起了...
```

---

## 設計決策記錄

### 已移除的元素
- ❌ 日期 emoji (📅)
- ❌ 筆記 emoji (📝)
- ❌ 「我的旅程」頁面功能

### 保留的元素
- ✅ 笑臉 emoji (😊) 在情緒標籤 Label
- ✅ 所有 6 種情緒選項
- ✅ 自動填入金句功能

### 間距調整歷史
- 留言框與情緒標籤間距: 1.5rem → 1rem → **0.5rem** (最終)
- 留言框高度: 120px → **160px** (最終)

---

## 維護說明

### 修改情緒選項
修改 `moods` 陣列 (第 19 行):
```javascript
const moods = ['震撼', '平靜', '浪漫', '孤獨', '感動', '其餘'];
```

### 修改字數限制
修改 `maxLength` 屬性 (第 67 行):
```javascript
maxLength={500}
```

### 修改初始內容來源
修改 `useState` 初始化 (第 12 行):
```javascript
const [note, setNote] = useState(existingFootprint?.note || arch?.quotes?.[0]?.text || '');
```

---

## 相關文件
- 主程式: [`App.jsx`](file:///Users/eyestudio/Documents/AILife/eyework/建築巡禮/義大利/網站系統/src/App.jsx)
- 樣式表: [`index.css`](file:///Users/eyestudio/Documents/AILife/eyework/建築巡禮/義大利/網站系統/src/index.css)
- 資料結構: `src/data/*.json`

---

**文件結束**
